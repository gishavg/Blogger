<span class="text-primary like" data-id="<%= current_user.id %>" post-id="<%= comment.id %>" data-type="Comment" remote=true>
  <% if comment.likes.count > 0 %><span><%= comment.likes.count %></span><% end %>
  <i class="fa fa-thumbs-o-up text-primary" style="font-size:20px;color:black"></i>
  <span class="likelable">Like</span>
</span>
