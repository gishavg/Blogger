<%= render :partial => '/comments/new', locals: {user_id: @post.user_id, post_id: @post.id} %>
<% comments = @post.comments.where("parent_id IS ?",nil) %>
<% if comments.length !=0 %>
  <ul style="list-style-type:none"><%= render comments %></ul>
<% end %>
